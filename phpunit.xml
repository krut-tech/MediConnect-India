<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="./vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="DB_CONNECTION" value="sqlite_testing"/>
        <env name="DB_TESTING_DATABASE" value=":memory:"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="SESSION_DRIVER" value="array"/>
        <!-- Test-only fixture values, never real credentials. Used solely
             to sign+verify a fake JWT within the same test process and to
             give Http::fake() something deterministic to match against. -->
        <env name="SUPABASE_URL" value="https://test-project.supabase.local"/>
        <env name="SUPABASE_ANON_KEY" value="test-anon-key-fixture"/>
        <env name="SUPABASE_JWT_SECRET" value="test-only-jwt-secret-fixture-not-real"/>
    </php>
</phpunit>
